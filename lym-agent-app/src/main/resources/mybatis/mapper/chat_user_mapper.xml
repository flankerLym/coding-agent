<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lym.ai.infrastructure.dao.IChatUserDao">

    <resultMap id="ChatUserMap" type="com.lym.ai.infrastructure.dao.po.ChatUser">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="mobile" property="mobile"/>
        <result column="avatar" property="avatar"/>
        <result column="role_code" property="roleCode"/>
        <result column="status" property="status"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="queryByUsername" resultMap="ChatUserMap">
        SELECT id, user_id, tenant_id, username, password_hash, nickname, email, mobile, avatar,
               role_code, status, last_login_time, create_time, update_time, deleted
        FROM chat_user
        WHERE tenant_id = #{tenantId}
          AND username = #{username}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="queryByUserId" resultMap="ChatUserMap">
        SELECT id, user_id, tenant_id, username, password_hash, nickname, email, mobile, avatar,
               role_code, status, last_login_time, create_time, update_time, deleted
        FROM chat_user
        WHERE user_id = #{userId}
          AND deleted = 0
        LIMIT 1
    </select>

    <update id="updateLastLoginTime">
        UPDATE chat_user
        SET last_login_time = NOW(),
            update_time = NOW()
        WHERE user_id = #{userId}
    </update>

</mapper>
